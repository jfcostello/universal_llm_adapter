{
  "name": "test.echo",
  "description": "Return the provided message reversed with a length prefix (format: [R:length]reversed_message). Used for integration testing.",
  "parametersJsonSchema": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "The message to echo back"
      }
    },
    "required": ["message"],
    "additionalProperties": false
  }
}
