# `plugins/modules/test-echo`

Test process module used by integration/live tests.

## Public API
- `handle(ctx)` exported from `index.ts`

## Notes
- Implementation details live in `internal/` and must not be imported directly outside this directory.

