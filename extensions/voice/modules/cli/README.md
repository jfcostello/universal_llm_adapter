# Voice CLI Module

Owns the `llm-adapter voice ...` CLI surface for the voice extension.

## Import rules
- Runtime code must import only from `extensions/voice/modules/cli/index.ts`.
- Do not import from `extensions/voice/modules/cli/internal/**` outside of this module.

