# Voice Logger Module

Owns structured file logging utilities for the voice extension (and log retention settings).

## Import rules
- Runtime code must import only from `extensions/voice/modules/logger/index.ts`.
- Do not import from `extensions/voice/modules/logger/internal/**` outside of this module.

