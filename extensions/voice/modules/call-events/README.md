# Voice Call Events Module

In-memory event hub for streaming per-call events (SSE) from the voice extension.

## Import rules
- Runtime code must import only from `extensions/voice/modules/call-events/index.ts`.
- Do not import from `extensions/voice/modules/call-events/internal/**` outside of this module.

