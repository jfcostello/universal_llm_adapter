# Voice Server Module

Owns the server registration and HTTP/WS handlers for the voice extension.

## Import rules
- Runtime code must import only from `extensions/voice/modules/server/index.ts`.
- Do not import from `extensions/voice/modules/server/internal/**` outside of this module.

